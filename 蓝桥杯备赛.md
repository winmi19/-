省1/国3：递归 & 回溯、==背包dp==、==线性dp==、==入门dp==、==网格图dp==、==记忆化搜索==、==二分答案==、==二分查找==、==网格图dfs/bfs问题==、双指针、滑动窗口、==排序&贪心==、前缀 & 差分、==字符串哈希==、欧拉筛、==质因数分解==、==并查集==、单调栈 & 单调队列、==堆==、==图上DFS/BFS==、==Dijkstra==、==Floyd ==

国2：区间dp、树形dp、状压dp、数位dp、最近公共祖先、字典树Trie树、Z函数、欧拉函数、裴蜀定理、线性同余方程、中国剩余定理、组合数学基础（容斥原理）、树状数组、朴素线段树、ST表、拓扑排序、最小生成树、二分图、倍增 & 构造、基环树问题、离散化


## 基础语法

字符串转列表：
list(s)  每一个字符都保留，包括空格
s.split()   默认空格
s.split(',')   不保留分隔符

列表转字符串：
"".join(list)
```

names = ['Python', 'Java', 'C++']
# 用空格粘合
s1 = " ".join(names)  # 结果: 'Python Java C++'

# 用逗号粘合
s2 = ",".join(names)  # 结果: 'Python,Java,C++'

# 直接粘合（无缝对接）
s3 = "".join(names)   # 结果: 'PythonJavaC++'
```
要求，列表里面只能有字符串，如果全是数字：
```
nums=[1,2,3]
s=" ".join(map(str,nums))
print(s)
```

print 输出：
```
a=1
b=2
c=3
print(a,b,c,sep='-',end=' ')
print()
print('hello')
```

必备：
```
import sys
input=lambda:sys.stdin.readline().rstrip()

s=input()
print(s)
```

lambda使用
```
sum_5=lambda x: x+5

print(sum_5(10))
```

列表：
```

num=[3,5,7,1,2]

num.sort(reverse=True)

print(num)

# print(sorted(num))

print(len(num))

print(sum(num))

print(max(num))

print(num.count(1))

输出:
[7, 5, 3, 2, 1]
5
18
7
1


```
进阶列表排序：
```
fruits=['apple','banana','cherry']

print(fruits)

sorted_fruits=sorted(fruits,key=lambda x:x[1])

print(sorted_fruits)
```

map：
	![[Pasted image 20260302114220.png]]

在你之前的图片例子中： `a, b, c = map(int, input().split())`
之所以没有报错，是因为你使用了**变量解包（Unpacking）**。当你把 `map` 对象赋值给固定数量的变量（`a, b, c`）时，Python 会强制要求 `map` 把里面的值一个个“吐”出来填满这些变量。

如果你直接打印它，你会看到类似 `<map object at 0x...>` 的东西，而不是你期待的 `[1, 2, 3]`。


矩阵的表示
```
n=3

matrix=[[0]*n for _ in range(n)]

nnn=[[0 for _ in range(n)]for _ in range(n)]

print(matrix)

print(nnn)

#构建矩阵
row=2

col=3

matrix=[(x,y) for x in range(row) for y in range(col)]

print(matrix)
```

队列：
```
from collections import deque

q=deque()

q.append(1)

q.append(2)

q.append(2)

q.popleft()

print(q)

print(list(q))

p=deque([7,8,9])

print(p)

p.popleft()

print(p)

输出：
deque([2, 2])
<class 'collections.deque'>
[2, 2]
deque([7, 8, 9])
deque([8, 9])
```

栈：
```
stk=[]

stk.append(1)

stk.append(2)

stk.append(3)

x=stk.pop()

print(stk)

print(x)
```



leetcode熟悉题型

codeforce强化思维


3.6写完孤岛题目和回溯题