要开发 Uni-app，你需要先安装一个神器：**HBuilderX**（这是开发 Uni-app 的官方 IDE，非常轻量）。

Uni-app 的伟大之处在于：你写一套 Vue 代码，点击“编译”，它能帮你生成 **iOS App、Android App、微信小程序和 H5 网页**。

---

### 1. 创建项目

1. 下载并打开 [HBuilderX](https://www.dcloud.io/hbuilderx.html)。
    
2. 点击 **文件 -> 新建 -> 项目**。
    
3. 选择 **uni-app**，模板选择“默认模板”，起个名字叫 `weather-app`。
    

### 2. 编写代码 (`pages/index/index.vue`)

在左侧目录找到 `pages/index/index.vue`，把里面的内容替换成下面这个。你会发现它和你之前的 Vue 代码**极其相似**，只是标签名变了（比如 `div` 变成了 `view`）：

HTML

```
<template>
	<view class="container">
		<view class="card">
			<text class="title">AI 天气助手 (移动端)</text>
			<view class="input-group">
				<input class="input" v-model="city" placeholder="输入城市拼音..." />
				<button class="btn" @click="getWeather" :disabled="loading">
					{{ loading ? '查询中...' : '查询' }}
				</button>
			</view>

			<view v-if="weather" class="result">
				<view class="item">🌡️ 温度：{{ weather.temperature }}</view>
				<view class="item">💡 建议：{{ weather.recommendation }}</view>
			</view>
		</view>
	</view>
</template>

<script setup>
import { ref } from 'vue';

const city = ref('');
const weather = ref(null);
const loading = ref(false);

const getWeather = async () => {
	if (!city.value) return;
	loading.value = true;
	
	try {
		// 注意：在真机测试时，不能写 localhost，必须写你电脑的局域网 IP！
		const res = await uni.request({
			url: `http://你的电脑IP:3000/weather/${city.value}`, 
			method: 'GET'
		});
		weather.value = res.data;
	} catch (err) {
		uni.showToast({ title: '查询失败', icon: 'none' });
	} finally {
		loading.value = false;
	}
};
</script>

<style>
.container { padding: 20px; background-color: #f5f5f5; height: 100vh; }
.card { background-color: #fff; padding: 20px; border-radius: 10px; }
.title { font-size: 20px; font-weight: bold; margin-bottom: 20px; display: block; text-align: center; }
.input-group { display: flex; margin-bottom: 20px; }
.input { border: 1px solid #ddd; padding: 10px; flex: 1; border-radius: 5px; height: 40px; }
.btn { background-color: #007aff; color: white; margin-left: 10px; font-size: 14px; line-height: 40px; }
.result { border-top: 1px solid #eee; pt: 20px; }
.item { margin-top: 10px; font-size: 16px; }
</style>
```

---

### 3. 如何查看结果？（关键步骤）

由于你在开发移动端，查看结果的方式有三种：

#### **方式 A：浏览器查看（最快）**

1. 在 HBuilderX 顶部菜单点击 **运行 -> 运行到浏览器 -> Chrome**。
    
2. 它会弹出一个网页，展示出手机 App 的样子。
    

#### **方式 B：微信小程序查看**

1. 在 HBuilderX 点击 **运行 -> 运行到小程序模拟器 -> 微信开发者工具**。
    
2. 你会看到你的代码瞬间变成了一个**微信小程序**。
    

#### **方式 C：手机真机查看（最酷）**

1. 确保手机和电脑连在**同一个 Wi-Fi**。
    
2. 在 HBuilderX 点击 **运行 -> 运行到设备 -> 运行到 Android/iOS 基座**。
    
3. **注意**：代码里的 `url` 必须改成你电脑的真实 IP（例如 `192.168.1.5`），不能用 `localhost`，因为手机理解的 `localhost` 是手机自己，不是你的电脑。
    

---

### ⚠️ 一个必须要避开的“后端大坑”：跨域 (CORS)

当你尝试从 Uni-app 或其他端访问你的 Node.js 后端时，浏览器可能会报错：`Access-Control-Allow-Origin`。这是为了安全，后端默认不让别的“域名”随便拿数据。

**解决办法**：在你的后端 `app.js` 里安装并使用 `cors` 插件。

1. 终端运行：`npm install cors`
    
2. 在 `app.js` 里加上：
    
    JavaScript
    
    ```
    const cors = require('cors');
    app.use(cors()); // 允许所有来源访问，这在开发阶段非常有用
    ```
    

---

### 💡 现在的感觉如何？

你已经从写一个简单的 API，进化到了能开发 **Web + App + 小程序** 的全栈雏形了。

**这就是后端的威力：你写好逻辑后，它就像一个“电源插座”，无论什么电器（网页、手机、小程序）插上来都能用。**

**你想让我教你如何把这个后端真正“上线”到互联网上，让你的朋友在他们自己家里也能搜到你的天气预报吗？**